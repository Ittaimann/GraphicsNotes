Transition/upload/uav textures are in memeory as linear. So it goes pixel 1,2,3,4,5...

However the textures for pxiel drawing might be swizzled to Morton order. So in memory it goes 1,2, next row 1,2. Because pixel work is done as a quad this maintains data locality when sampling the texture.
